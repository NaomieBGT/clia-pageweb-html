<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>CLIA - Contact</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="icon" type="image/png" href="LOGOONOIR.png"/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --max-width:1200px;
      --header-height:84px;
      --card-bg: rgba(0, 0, 0, 0.095);
      --muted:#ededed;
      --accent:#d5bc03fa;
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color:#111;
      background: #000;
    }

    /* Full-screen animated background (contactmur.gif) with overlay */
    .bg{
      position:fixed;
      inset:0;
      background: url("contactmur.gif") center/cover no-repeat fixed;
      z-index:0;
    }
    .overlay{
      position:fixed;
      inset:0;
      background: linear-gradient(180deg, rgba(0,0,0,0.45), rgba(0,0,0,0.55));
      z-index:1;
      backdrop-filter: blur(2px);
    }

    /* Header (same as other pages) */
    header{
      position:fixed;
      left:0;right:0;top:0;
      height:var(--header-height);
      z-index:1002;
      background: linear-gradient(0deg, rgba(0,0,0,0.35), rgba(0,0,0,0.35));
      display:flex;
      align-items:center;
    }
    .container{
      width:100%;
      max-width:var(--max-width);
      margin:0 auto;
      padding:0 28px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:20px;
      z-index:1003;
    }
    .logo img{ height:210px; width:auto; display:block; }

    nav{ display:flex; gap:18px; font-family:"Playfair Display", serif; }
    nav a{ color:#fff; text-decoration:none; font-size:15px; padding:6px 10px; border-radius:8px; }

    /* Content area */
    main{
      position:relative;
      z-index:1003;
      padding-top: calc(var(--header-height) + 40px);
      padding-bottom:60px;
      max-width:var(--max-width);
      margin:0 auto;
      padding-left:28px;
      padding-right:28px;
      color: #fff;
    }

    .card{
      background: var(--card-bg);
      color:#ffd336c9;
      border-radius:12px;
      padding:22px;
      box-shadow: 0 12px 40px rgba(0,0,0,0.45);
      border: 1px solid rgba(255, 0, 225, 0.06);
      max-width:880px;
      margin:40px auto;
    }

    .card h2{
      font-family:"Playfair Display", serif;
      margin:0 0 8px 0;
      font-size:22px;
    }
    .card p.lead{ margin:0 0 20px 0; color:var(--muted); font-size:15px; }

    .team{
      display:grid;
      grid-template-columns: repeat(2, minmax(0,1fr));
      gap:18px;
      margin-top:12px;
    }
    .member{
      background: #14141400;
      border-radius:10px;
      padding:12px 14px;
      box-shadow: 0 6px 20px rgba(193, 193, 193, 0.282);
    }
    .member h3{ margin:0; font-size:16px; font-family:"Playfair Display", serif; }
    .member p{ margin:6px 0 0 0; font-size:14px; color:var(--muted); line-height:1.4; }

    .contact-general{
      margin-top:18px;
      padding-top:14px;
      border-top:1px dashed rgba(0,0,0,0.06);
      font-size:14px;
    }

    footer{
      margin-top:30px;
      text-align:center;
      color: rgba(248, 248, 248, 0.944);
      font-size:13px;
    }

    /* Responsive */
    @media (max-width:920px){
      .team{ grid-template-columns: 1fr; }
      .card{ margin:20px; padding:18px; }
    }
  </style>
</head>
<body>
  <div class="bg" aria-hidden="true"></div>
  <div class="overlay" aria-hidden="true"></div>

  <header>
    <div class="container">
      <a class="logo" href="clia.accueil.html"><img src="logo.transparent.grand.png" alt="Logo CLIA"></a>
      <nav aria-label="Navigation principale">
        <a href="clia.accueil.html">Accueil</a>
        <a href="fonctionnalite.html">Fonctionnalités</a>
        <a href="abonnement.html" style="opacity:0.95;">Abonnement</a>
        <a href="contact.html">Contact</a>
      </nav>
    </div>
  </header>


  <main>
    <section class="card" aria-labelledby="contact-title">
      <h2 id="contact-title">Contact & Équipe</h2>
      <p class="lead">Nous sommes à votre disposition pour toute question, démonstration ou partenariat. Voici les contacts clés chez CLIA.</p>

      <div class="team" role="list">
        <div class="member" role="listitem">
          <h3>Naomie BAGATE</h3>
          <p><em>Fondatrice & Directrice Technique (CTO)</em></p>
          <p>Spécialisée en développement full‑stack & intelligence artificielle.<br>
          📧 <a href="mailto:naomie.bagate@clia-tech.com">naomie.bagate@clia-tech.com</a></p>
        </div>

        <div class="member" role="listitem">
          <h3>Cerena MICHAUD</h3>
          <p><em>Co‑fondatrice & Directrice Communication</em></p>
          <p>Responsable communication, identité visuelle et relations externes.<br>
          📧 <a href="mailto:cerena.michaud@clia-tech.com">cerena.michaud@clia-tech.com</a></p>
        </div>

        <div class="member" role="listitem">
          <h3>Nathalie ONEISSI</h3>
          <p><em>Co‑fondatrice & Directrice Financière (CFO)</em></p>
          <p>En charge du financement, modélisation économique et stratégie financière.<br>
          📧 <a href="mailto:nathalie.oneissi@clia-tech.com">nathalie.oneissi@clia-tech.com</a></p>
        </div>

        <div class="member" role="listitem">
          <h3>Seating SYLLA</h3>
          <p><em>Co‑fondatrice & Directrice Logistique</em></p>
          <p>Responsable des processus logistiques, conformité documentaire et opérations.<br>
          📧 <a href="mailto:seating.sylla@clia-tech.com">seating.sylla@clia-tech.com</a></p>
        </div>
      </div>

      <div class="contact-general">
        <p><strong>Contact général :</strong><br>📧 <a href="mailto:contact@clia-tech.com">contact@clia-tech.com</a></p>
      </div>

      <footer>
        © 2025 CLIA. Tous droits réservés.
      </footer>
    </section>
  </main>
</body>
</html>
